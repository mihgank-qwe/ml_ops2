# Скопируйте в terraform.tfvars и заполните значения.
# terraform.tfvars не коммитить (добавьте в .gitignore)!

environment        = "staging"
zone               = "ru-central1-a"
yandex_cloud_id    = "your-cloud-id"
yandex_folder_id   = "your-folder-id"

# Storage
artifacts_bucket_name = "your-unique-org-credit-scoring-artifacts"

# GPU node group (опционально)
# enable_gpu_nodes = true
# gpu_cluster_id   = "your-gpu-cluster-id"
