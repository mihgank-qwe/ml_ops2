# Secret для чувствительных данных.
# В production: создавать через kubectl create secret или external-secrets.
# Пример: kubectl create secret generic credit-scoring-api-secret --from-literal=api-key=xxx
apiVersion: v1
kind: Secret
metadata:
  name: credit-scoring-api-secret
  labels:
    app: credit-scoring-api
type: Opaque
stringData:
  # Заглушка - заменить на реальные значения
  placeholder: 'replace-in-production'
